# Reference voices

Add only reference voices that you have permission to use.

- `default.wav`: fallback voice used when a named voice is unavailable.
- `<voice>.wav`: optional named voices such as `coral.wav` or `nova.wav`.

Use clean mono speech without music or background noise. A short, natural sentence is
enough for an initial test. Do not commit voice recordings to Git.
